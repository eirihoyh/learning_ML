# Shim to wrap setup.py invocation with setuptools
SETUPTOOLS_SHIM = (
    "import setuptools, tokenize;__file__=%r;"
    "f=getattr(tokenize, 'open', open)(__file__);"
    "basic_ml=f.read().replace('\\r\\n', '\\n');"
    "f.close();"
    "exec(compile(basic_ml, __file__, 'exec'))"
)
